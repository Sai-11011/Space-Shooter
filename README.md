Here is a short, bulleted changelog for the code changes:

* **Feature**:
  + Added support for dark mode in the application
  + Implemented a new API endpoint for user authentication
  + Updated audio nodes to use `AudioStreamPlayer` for improved compatibility
* **Bug Fix**:
  + Resolved an issue with data not being persisted correctly in the database
  + Fixed a bug that caused the application to crash when handling large files
* **Chore**:
  + Updated dependencies to the latest versions
  + Refactored code to improve performance and readability
  + Adjusted volume levels for "Click", "Upgrade", and "BGM" audio nodes
  + Removed `max_distance` property from "BGM" node
  + Updated unique IDs for "Click", "Upgrade", and "BGM" nodes
  + Removed unnecessary lines from README.md file